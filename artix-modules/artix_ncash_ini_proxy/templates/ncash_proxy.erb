;-- Файл был сгенерирован сервером puppet
[Proxy]
; Настройки прокси сервера

; Имя хоста
hostName = <%= @proxy_hostname %>

; Порт. 3128 по-умолчанию.
port = <%= @proxy_port %>

; Параметры user и password не обязательны. Устанавливаются, в случае,
; если необходимо установить ограничения на пользование прокси-сервером.
; Логин
user = <%= @proxy_user %>

; Пароль
password = <%= @proxy_password %>
